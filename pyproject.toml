[project]
name = "drag-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "firebase-admin==6.2.0",
    "flask==2.3.3",
    "flask-cors==4.0.0",
    "gunicorn==21.2.0",
    "python-dotenv==1.0.0",
]
